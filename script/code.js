let numbers = [12, 18, 11, 5, 8, 2];

let middleNumber = Math.floor(numbers.length/2)


if (numbers.length % 2 == 0) {
    console.log('middle numbers:', numbers[middleNumber -1], numbers[middleNumber]);
    
} 
